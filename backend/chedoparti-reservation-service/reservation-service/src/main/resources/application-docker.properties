spring.datasource.url=jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:reservation_service_db}

# OpenAPI Configuration - Use API Gateway URL for Docker environment
# The API Gateway routes requests to this service, so OpenAPI docs should point to the gateway
openapi.service.url=http://api-gateway:8989
